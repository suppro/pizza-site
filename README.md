# Интернет-магазин АО «Арвиай»

Современный интернет-магазин для оптово-розничной продажи авиационных запчастей, комплектующих и сопутствующего оборудования с интеграцией с системой 1С:Предприятие.

## 🚀 Возможности

- **Каталог товаров** с иерархической структурой категорий
- **Интеллектуальный поиск** по названию, артикулу и описанию
- **Система фильтрации** по категориям, цене и наличию
- **Корзина покупок** с управлением количеством товаров
- **Трехшаговое оформление заказа** с валидацией данных
- **Личный кабинет клиента** с историей заказов
- **Административная панель** для управления товарами, категориями, заказами и пользователями
- **Интеграция с 1С:Предприятие** для автоматической выгрузки заказов
- **Email-уведомления** об изменении статуса заказа
- **Адаптивный дизайн** для всех типов устройств

## 📋 Требования

Для работы проекта необходимы следующие компоненты:

- **PHP >= 8.1** - язык программирования для серверной части
- **Composer** - менеджер зависимостей для PHP
- **MySQL >= 5.7** или **MariaDB >= 10.3** - система управления базами данных
- **Node.js >= 16.x** и **npm** - для сборки фронтенд-ресурсов
- **Git** - система контроля версий (опционально, если клонируете репозиторий)

## 💻 Установка необходимого программного обеспечения

### Шаг 1: Установка XAMPP

XAMPP включает в себя Apache, MySQL, PHP и phpMyAdmin - все необходимое для работы Laravel приложения.

1. **Скачайте XAMPP:**
   - Перейдите на официальный сайт: https://www.apachefriends.org/
   - Выберите версию для Windows (рекомендуется PHP 8.1 или выше)
   - Скачайте установщик (например, `xampp-windows-x64-8.1.x-installer.exe`)

2. **Установите XAMPP:**
   - Запустите установщик от имени администратора
   - Выберите компоненты для установки (Apache, MySQL, PHP, phpMyAdmin - все должны быть отмечены)
   - Выберите путь установки (по умолчанию `C:\xampp`)
   - Завершите установку

3. **Запустите XAMPP Control Panel:**
   - Откройте XAMPP Control Panel из меню Пуск
   - Нажмите "Start" для модулей **Apache** и **MySQL**
   - Убедитесь, что оба модуля запущены (зеленый индикатор)

4. **Проверка установки:**
   - Откройте браузер и перейдите по адресу: `http://localhost`
   - Должна открыться страница XAMPP
   - Перейдите по адресу: `http://localhost/phpmyadmin`
   - Должен открыться phpMyAdmin

### Шаг 2: Установка Composer

Composer необходим для установки PHP-зависимостей проекта.

1. **Скачайте Composer:**
   - Перейдите на официальный сайт: https://getcomposer.org/download/
   - Скачайте установщик `Composer-Setup.exe`

2. **Установите Composer:**
   - Запустите установщик
   - Установщик автоматически найдет PHP (из XAMPP)
   - Если не найдет, укажите путь вручную: `C:\xampp\php\php.exe`
   - Завершите установку

3. **Проверка установки:**
   - Откройте командную строку (cmd) или PowerShell
   - Выполните команду: `composer --version`
   - Должна отобразиться версия Composer (например, `Composer version 2.x.x`)

### Шаг 3: Установка Node.js и npm

Node.js необходим для сборки фронтенд-ресурсов (CSS и JavaScript).

1. **Скачайте Node.js:**
   - Перейдите на официальный сайт: https://nodejs.org/
   - Скачайте LTS версию (рекомендуется версия 18.x или выше)
   - Выберите установщик для Windows (например, `node-v18.x.x-x64.msi`)

2. **Установите Node.js:**
   - Запустите установщик
   - Следуйте инструкциям мастера установки
   - Убедитесь, что опция "Add to PATH" отмечена
   - Завершите установку

3. **Проверка установки:**
   - Откройте новую командную строку (cmd) или PowerShell
   - Выполните команды:
     ```bash
     node --version
     npm --version
     ```
   - Должны отобразиться версии Node.js и npm

### Шаг 4: Установка Git (опционально)

Git необходим только если вы клонируете проект из репозитория.

1. **Скачайте Git:**
   - Перейдите на официальный сайт: https://git-scm.com/download/win
   - Скачайте установщик для Windows

2. **Установите Git:**
   - Запустите установщик
   - Следуйте инструкциям мастера установки
   - Оставьте настройки по умолчанию

3. **Проверка установки:**
   - Откройте командную строку
   - Выполните команду: `git --version`
   - Должна отобразиться версия Git

## 🔧 Установка проекта

### Шаг 1: Получение файлов проекта

**Вариант А: Клонирование из репозитория (если проект в Git)**

1. Откройте командную строку или PowerShell
2. Перейдите в папку, где хотите разместить проект (например, `C:\xampp\htdocs`)
3. Выполните команду:
   ```bash
   git clone https://github.com/your-username/arviay-shop.git
   cd arviay-shop
   ```

**Вариант Б: Распаковка архива**

1. Распакуйте архив с проектом в папку `C:\xampp\htdocs\arviay-shop`
2. Откройте командную строку или PowerShell
3. Перейдите в папку проекта:
   ```bash
   cd C:\xampp\htdocs\arviay-shop
   ```

### Шаг 2: Добавление PHP в PATH (если еще не добавлен)

Для удобства работы с командами Laravel добавьте PHP в переменную PATH:

1. Откройте "Параметры системы" → "Дополнительные параметры системы" → "Переменные среды"
2. В разделе "Системные переменные" найдите переменную `Path` и нажмите "Изменить"
3. Нажмите "Создать" и добавьте путь: `C:\xampp\php`
4. Нажмите "ОК" во всех окнах
5. **Важно:** Закройте и заново откройте командную строку/PowerShell

**Проверка:**
```bash
php --version
```
Должна отобразиться версия PHP (например, `PHP 8.1.x`)

### Шаг 3: Установка зависимостей PHP

1. Убедитесь, что вы находитесь в папке проекта
2. Выполните команду:
   ```bash
   composer install
   ```
3. Дождитесь завершения установки (может занять несколько минут)
4. Убедитесь, что появилась папка `vendor` в корне проекта


**Если возникает ошибка:**
- Убедитесь, что Composer установлен: `composer --version`
- Проверьте подключение к интернету
- Попробуйте выполнить: `composer install --no-interaction`

### Шаг 4: Установка зависимостей Node.js

1. Убедитесь, что вы находитесь в папке проекта
2. Выполните команду:
   ```bash
   npm install
   ```
3. Дождитесь завершения установки (может занять несколько минут)
4. Убедитесь, что появилась папка `node_modules` в корне проекта

**Если возникает ошибка:**
- Убедитесь, что Node.js установлен: `node --version`
- Попробуйте выполнить: `npm install --legacy-peer-deps`

### Шаг 5: Настройка окружения

1. **Создайте файл `.env`:**
   - В папке проекта найдите файл `.env.example`
   - Скопируйте его и переименуйте в `.env`
   - В Windows можно использовать команду:
     ```bash
     copy .env.example .env
     ```

2. **Откройте файл `.env` в текстовом редакторе** (Notepad++, VS Code, или обычный Блокнот)

3. **Настройте основные параметры:**
   ```env
   APP_NAME="АО «Арвиай»"
   APP_ENV=local
   APP_KEY=
   APP_DEBUG=true
   APP_URL=http://localhost:8000
   ```

4. **Настройте подключение к базе данных:**
   ```env
   DB_CONNECTION=mysql
   DB_HOST=127.0.0.1
   DB_PORT=3306
   DB_DATABASE=arviay_shop
   DB_USERNAME=root
   DB_PASSWORD=
   ```
   
   **Примечание:** По умолчанию в XAMPP:
   - Имя пользователя: `root`
   - Пароль: пустой (оставьте `DB_PASSWORD=` без значения)

5. **Сохраните файл `.env`**

### Шаг 6: Генерация ключа приложения

Выполните команду:
```bash
php artisan key:generate
```

**Проверка:** Откройте файл `.env` и убедитесь, что в строке `APP_KEY=` появилось значение (например, `APP_KEY=base64:...`)

### Шаг 7: Создание базы данных

**Способ 1: Через phpMyAdmin (рекомендуется для начинающих)**

1. Откройте браузер и перейдите по адресу: `http://localhost/phpmyadmin`
2. В левом меню нажмите "Создать" (New)
3. В поле "Имя базы данных" введите: `arviay_shop`
4. В выпадающем списке "Сравнение" выберите: `utf8mb4_unicode_ci`
5. Нажмите "Создать"
6. База данных должна появиться в левом меню

**Способ 2: Через командную строку MySQL**

1. Откройте командную строку
2. Перейдите в папку MySQL XAMPP:
   ```bash
   cd C:\xampp\mysql\bin
   ```
3. Подключитесь к MySQL:
   ```bash
   mysql.exe -u root
   ```
4. Выполните SQL-команду:
   ```sql
   CREATE DATABASE arviay_shop CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```
5. Выйдите из MySQL:
   ```sql
   exit;
   ```

**Проверка:** В phpMyAdmin должна появиться база данных `arviay_shop`

### Шаг 8: Выполнение миграций

1. Убедитесь, что вы находитесь в папке проекта
2. Убедитесь, что MySQL запущен в XAMPP Control Panel
3. Выполните команду:
   ```bash
   php artisan migrate
   ```
4. Дождитесь завершения (должны создаться все таблицы)
5. **Проверка:** Откройте phpMyAdmin, выберите базу данных `arviay_shop` - должны появиться таблицы: `users`, `categories`, `products`, `orders`, и т.д.

**Если возникает ошибка:**
- Убедитесь, что MySQL запущен в XAMPP
- Проверьте настройки подключения в `.env`
- Убедитесь, что база данных `arviay_shop` создана

### Шаг 9: Заполнение базы данных тестовыми данными

Выполните команду:
```bash
php artisan db:seed
```

Это создаст:
- **Администратора** (email: `admin@arviay.ru`, пароль: `admin123`)
- **Тестового клиента** (email: `client@arviay.ru`, пароль: `client123`)
- **Категории товаров** (4 основные категории + подкатегории)
- **Товары** с полными характеристиками и изображениями

**Проверка:** В phpMyAdmin в таблице `users` должны появиться 2 пользователя

### Шаг 10: Создание символической ссылки для хранения

Выполните команду:
```bash
php artisan storage:link
```

Это создаст символическую ссылку для доступа к загруженным изображениям товаров.

**Проверка:** В папке `public` должна появиться папка `storage`, которая является ссылкой на `storage/app/public`

### Шаг 11: Сборка фронтенд-ресурсов

**Для разработки (с автоматической пересборкой при изменениях):**

1. Выполните команду:
   ```bash
   npm run dev
   ```
2. Оставьте это окно открытым - оно будет автоматически пересобирать CSS и JavaScript при изменениях

**Для продакшена (одноразовая сборка):**

Выполните команду:
```bash
npm run build
```

**Проверка:** В папке `public/build` должны появиться файлы `assets/app-*.css` и `assets/app-*.js`

### Шаг 12: Запуск сервера разработки

1. **Откройте новое окно командной строки или PowerShell** (если `npm run dev` запущен, оставьте его работать)

2. **Перейдите в папку проекта:**
   ```bash
   cd C:\xampp\htdocs\arviay-shop
   ```

3. **Запустите встроенный сервер Laravel:**
   ```bash
   php artisan serve
   ```

4. **Проверка:** В консоли должно появиться сообщение:
   ```
   INFO  Server running on [http://127.0.0.1:8000]
   ```

5. **Откройте браузер** и перейдите по адресу: `http://localhost:8000`

6. **Должна открыться главная страница** интернет-магазина АО «Арвиай»

## ✅ Проверка установки

После выполнения всех шагов проверьте следующее:

1. **Главная страница открывается:** `http://localhost:8000`
2. **Можно зарегистрироваться или войти:** `http://localhost:8000/login`
3. **Административная панель доступна:** `http://localhost:8000/admin/dashboard`
   - Войдите как администратор: `admin@arviay.ru` / `admin123`
4. **Каталог товаров отображается:** `http://localhost:8000/dashboard`
   - Войдите как клиент: `client@arviay.ru` / `client123`
5. **Изображения товаров отображаются** (если были загружены)

## 🚀 Запуск проекта в дальнейшем

После первой установки для запуска проекта достаточно:

1. **Запустить XAMPP Control Panel** и запустить модули **Apache** и **MySQL**

2. **Открыть два окна терминала:**

   **Терминал 1** (для сборки фронтенда):
   ```bash
   cd C:\xampp\htdocs\arviay-shop
   npm run dev
   ```

   **Терминал 2** (для сервера Laravel):
   ```bash
   cd C:\xampp\htdocs\arviay-shop
   php artisan serve
   ```

3. **Открыть браузер:** `http://localhost:8000`

## 👤 Учетные записи по умолчанию

После выполнения `php artisan db:seed` будут созданы следующие учетные записи:

### Администратор
- **Email:** `admin@arviay.ru`
- **Пароль:** `admin123`
- **Роль:** Администратор
- **Доступ:** Полный доступ к административной панели

### Клиент
- **Email:** `client@arviay.ru`
- **Пароль:** `client123`
- **Роль:** Клиент
- **Доступ:** Каталог товаров, корзина, оформление заказов

⚠️ **Важно:** После первого входа обязательно измените пароли на более надежные!

## ⚙️ Настройка интеграции с 1С

Для настройки интеграции с системой 1С:Предприятие добавьте в файл `.env` следующие параметры:

```env
ONEC_ENABLED=true
ONEC_BASE_URL=http://your-1c-server:port
ONEC_USERNAME=your_username
ONEC_PASSWORD=your_password
```

**Примечание:** Если интеграция с 1С не требуется на этапе разработки, установите `ONEC_ENABLED=false`.

### Ручная синхронизация заказов с 1С

Для ручной выгрузки всех невыгруженных заказов в 1С используйте команду:

```bash
php artisan 1c:sync-orders
```

Для выгрузки конкретного заказа:

```bash
php artisan 1c:sync-orders --order-id=123
```

## 📧 Настройка email-уведомлений

Для отправки email-уведомлений об изменении статуса заказа настройте SMTP в файле `.env`:

```env
MAIL_MAILER=smtp
MAIL_HOST=smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=your_username
MAIL_PASSWORD=your_password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@arviay.ru
MAIL_FROM_NAME="${APP_NAME}"
```

Для тестирования можно использовать [Mailtrap](https://mailtrap.io/) или [MailHog](https://github.com/mailhog/MailHog).

## 📁 Структура проекта

```
arviay-shop/
├── app/
│   ├── Http/
│   │   └── Controllers/
│   │       ├── Admin/          # Контроллеры административной панели
│   │       ├── Auth/           # Контроллеры аутентификации
│   │       ├── CartController.php
│   │       ├── CatalogController.php
│   │       ├── OrderController.php
│   │       └── ProductController.php
│   ├── Models/                 # Eloquent модели
│   ├── Services/
│   │   └── OneCService.php     # Сервис интеграции с 1С
│   ├── Helpers/
│   │   └── OrderHelper.php     # Вспомогательные функции
│   └── Notifications/
│       └── OrderStatusChanged.php
├── database/
│   ├── migrations/             # Миграции базы данных
│   └── seeders/                # Сидеры для заполнения данных
├── resources/
│   ├── views/                  # Blade шаблоны
│   │   ├── admin/              # Шаблоны административной панели
│   │   ├── auth/               # Шаблоны авторизации
│   │   └── partials/           # Частичные шаблоны
│   ├── css/
│   │   └── app.css             # Основные стили
│   └── js/
│       └── app.js              # JavaScript файлы
├── routes/
│   └── web.php                 # Веб-маршруты
└── storage/
    └── app/
        └── public/
            └── product_images/ # Загруженные изображения товаров
```

## 🗄️ База данных

### Основные таблицы

- `users` - Пользователи системы (администраторы и клиенты)
- `categories` - Иерархическая структура категорий товаров
- `products` - Товары с техническими характеристиками
- `product_images` - Изображения товаров
- `orders` - Заказы клиентов
- `order_items` - Позиции заказов

### Миграции

Все миграции находятся в директории `database/migrations/`. Для отката последней миграции:

```bash
php artisan migrate:rollback
```

Для отката всех миграций:

```bash
php artisan migrate:reset
```

## 🎨 Разработка

### Запуск в режиме разработки

1. Запустите сборку фронтенд-ресурсов в watch-режиме:

```bash
npm run dev
```

2. В другом терминале запустите сервер разработки:

```bash
php artisan serve
```

### Очистка кэша

При возникновении проблем с кэшем выполните:

```bash
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear
```

### Режим отладки

Для включения подробных сообщений об ошибках установите в `.env`:

```env
APP_DEBUG=true
```

⚠️ **Важно:** В продакшене всегда устанавливайте `APP_DEBUG=false`!

## 🚀 Развертывание в продакшене

### 1. Оптимизация для продакшена

```bash
composer install --optimize-autoloader --no-dev
npm run build
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

### 2. Настройка веб-сервера

Настройте веб-сервер (Nginx или Apache) для работы с Laravel. Пример конфигурации Nginx:

```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/arviay-shop/public;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";

    index index.php;

    charset utf-8;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }

    error_page 404 /index.php;

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.1-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}
```

### 3. Права доступа

Установите правильные права доступа:

```bash
chmod -R 755 storage bootstrap/cache
chown -R www-data:www-data storage bootstrap/cache
```

### 4. Переменные окружения

Убедитесь, что в `.env` установлены правильные значения для продакшена:

```env
APP_ENV=production
APP_DEBUG=false
APP_URL=https://your-domain.com
```

## 📝 Команды Artisan

### Управление заказами

```bash
# Синхронизация заказов с 1С
php artisan 1c:sync-orders

# Синхронизация конкретного заказа
php artisan 1c:sync-orders --order-id=123
```

### Работа с базой данных

```bash
# Выполнение миграций
php artisan migrate

# Откат последней миграции
php artisan migrate:rollback

# Заполнение базы данных
php artisan db:seed

# Выполнение миграций и заполнение данных
php artisan migrate --seed
```

## 🐛 Решение проблем

### Проблема: Изображения товаров не отображаются

**Решение:**
1. Убедитесь, что выполнена команда `php artisan storage:link`
2. Проверьте права доступа к директории `storage/app/public/product_images`
3. Убедитесь, что изображения загружены в правильную директорию

### Проблема: Ошибка "This password does not use the Bcrypt algorithm"

**Решение:**
Выполните SQL-скрипт для обновления паролей (см. файл `docs/project/reset_passwords.sql`) или создайте новых пользователей через административную панель.

### Проблема: Ошибка при интеграции с 1С

**Решение:**
1. Проверьте настройки в `.env` (ONEC_ENABLED, ONEC_BASE_URL, ONEC_USERNAME, ONEC_PASSWORD)
2. Убедитесь, что сервер 1С доступен по указанному адресу
3. Проверьте логи в `storage/logs/laravel.log`

### Проблема: Email-уведомления не отправляются

**Решение:**
1. Проверьте настройки SMTP в `.env`
2. Убедитесь, что очередь обрабатывается: `php artisan queue:work`
3. Проверьте логи в `storage/logs/laravel.log`

## 📚 Дополнительная документация

- [Руководство по настройке](docs/project/SETUP.md)
- [Руководство по миграции](docs/project/MIGRATION_GUIDE.md)
- [Интеграция с 1С](docs/project/ONEC_INTEGRATION.md)
- [Настройка email](docs/project/EMAIL_SETUP.md)
- [Список изменений](docs/project/CHANGELOG.md)
- [Реализованные функции](docs/project/FEATURES_IMPLEMENTED.md)
